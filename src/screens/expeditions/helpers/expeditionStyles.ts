import { css } from '@emotion/react'
import { greenBox } from '../../../styles/base'

export const expeditionStepDescription = [
  greenBox,
  css({
    marginBottom: '0.8rem',
    paddingTop: '1.8rem',
    fontSize: '0.9rem',
  }),
]
