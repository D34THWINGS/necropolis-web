import { css } from '@emotion/core'
import { greenBox } from '../../../styles/base'

export const expeditionStepDescription = [
  greenBox,
  css({
    marginBottom: '0.4rem',
    paddingTop: '1.8rem',
    fontSize: '0.9rem',
  }),
]
